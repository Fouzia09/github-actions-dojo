---
name: CI

on:
  push:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - run: echo "example"

  lint-all:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: github/super-linter@v4
        env:
          VALIDATE_ALL_CODEBASE: false


  